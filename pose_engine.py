def get_keypoints(frame_bgr):
    """
    Stub function for pose engine.
    Returns a dict of keypoints with format:
      {"left_hip": (x,y,conf), ...}
    For now, returns an empty dict to let the scaffold run.
    """
    return {}
