def compare_to_benchmark(user_reps: int, exercise: str, age: str, gender: str) -> dict:
    """
    Compare user_reps to data/benchmarks.csv (not yet filled).
    For now returns placeholders.
    """
    return {"user_reps": user_reps, "avg_reps": 0, "percent_diff": 0.0}
